
<nav id="nav-bar">
  <a class="navbar-brand" href="/">Wiki Map</a>

  <% if (!user.id) { %>
    <div class="functions">
      <a class="btn btn-outline-dark" href="/login/1" role="button">Login</a> &nbsp &nbsp
      <form method="POST" action="/register/new">
        <button class="btn btn-outline-dark" type="submit">Register</button>
      </form>
    </div>
      <% } else { %>
    <div class="other-functions">
      <form method="POST" action="/logout">
        <label for="email"><strong>Welcome <%= user.username %>!</strong></label> &nbsp
        <button type="submit" class="btn btn-outline-dark">Logout</button>
      </form> &nbsp
      <form method="GET" action="/profile">
        <button type="submit" class="btn btn-outline-dark" role="button">Profile</a>
      </form>
    </div>

  <% } %>
</nav>

